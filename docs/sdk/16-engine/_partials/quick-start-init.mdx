import {CLI_BINARY} from '/src/constants/env.ts';
import EngineRuntimes from '/src/docComponents/MultiLang/engine';
import {Conditional} from '/src/docComponents/conditional';
import TabItem from '@theme/TabItem';

<Conditional if={!props.noCliSetup}>

请先根据 [命令行工具使用指南 § 安装](/sdk/engine/cli#安装) 安装最新版本的命令行工具，然后根据 [命令行工具使用指南 § 登录帐号](/sdk/engine/cli#登录帐号) 登录到你的帐号。

</Conditional>

<p>如果你还没有在控制台创建过应用，请先在控制台创建应用，然后使用 <code>{CLI_BINARY} init</code> 创建项目：</p>

<pre>
<CodeBlock>
{`$ ${CLI_BINARY} init
[?] Please select an app:
 1) ${props.appName || 'my-engine-app'}
 => 1
[?] Please select a language
 1) Node.js
 2) Python
 3) Java
 4) PHP
 5) .Net
 6) Go
 7) Others
 => 1
[?] Please select an app template:
 1) Express
 2) Koa
 => 1
[INFO] Downloading templates 5.49 KiB / 5.49 KiB [==================] 100.00% 0s
[INFO] Creating project...
[INFO] Creating Express succeeded. Please refer to our website http://expressjs.com for documentation about Express`}
</CodeBlock>
</pre>

<p><code>{CLI_BINARY} init</code> 会创建一个与你的应用同名的目录，我们 <code>cd {props.appName || 'my-engine-app'}</code> 然后安装项目依赖：</p>

<EngineRuntimes only={props.only}>
<TabItem value='nodejs'>

```sh
npm install
```

</TabItem>
<TabItem value='python'>

```sh
pip install -Ur requirements.txt
```

</TabItem>
<TabItem value='php'>

```sh
composer install
```

</TabItem>
<TabItem value='java'>

```sh
mvn package
```

</TabItem>
<TabItem value='dotnet'>
需要安装 global.json 文件中指定的 .Net SDK 版本
</TabItem>
<TabItem value='go'>

```sh
go mod tidy
```

</TabItem>
</EngineRuntimes>

<Conditional if={!props.noCustomDomain}>

你可以在控制台绑定云引擎域名，绑定域名后，即可通过绑定域名访问你的应用。例如你在控制台绑定了 `web.example.com` 这个域名，即可通过 `https://web.example.com` 访问你的应用（生产环境）。

</Conditional>
