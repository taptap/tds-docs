import { HAS_SUB_DOMAIN } from "/src/constants/env.ts";
import { Conditional } from "/src/docComponents/conditional";
import Path from "/src/docComponents/path";

云引擎需要设置域名才能访问。在 **<Path to="engine" /> > 管理部署 > 你的分组 > 设置 > 访问域名** 处可以绑定域名。

<Conditional if={!props.noStaging}>

如果你绑定的域名以 `stg-` 开头（如 `stg-api.example.com`），会自动关联到预备环境。

</Conditional>

<Conditional if={HAS_SUB_DOMAIN}>

对于测试阶段的应用，我们提供了共享域名，你可以自定义共享域名的前缀部分。

</Conditional>
