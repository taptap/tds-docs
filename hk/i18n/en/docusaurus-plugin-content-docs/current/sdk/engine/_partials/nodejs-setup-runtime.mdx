import { Conditional } from "/src/docComponents/conditional";

You can specify the version of Node.js by setting `engines.node` in `package.json`:

```json title='package.json'
{
  "engines": {
    "node": "16.x"
  }
}
```

You can set it to `*` to stick to the latest (current) version.

:::note

<Conditional brand="leancloud">For newly created apps, if</Conditional>
<Conditional brand="tds">If</Conditional> you don’t specify a Node.js version,
the latest stable (LTS) version will be used.
<Conditional brand="leancloud">
  {" "}
  For apps created before 9/2/2021, Node.js `0.12` will be used by default to ensure
  compatibility.
</Conditional>

:::
